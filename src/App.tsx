import React from "react";
import "./App.scss";
import Header from "./components/header/Header";

function App() {
  return (
    <div className='App'>
      {/* Header */}
      <Header />

      {/* App Body */}
      {/* Side-bar (left side) */}
      {/* Feed */}
      {/* Widgets (right side) */}
    </div>
  );
}

export default App;
